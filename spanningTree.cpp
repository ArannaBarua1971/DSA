// **spanning TRee rules:
// **0. this is graph
// **1. n nodes and n-1 edges
// **2. we can convert in tree this graph
// **3. we can reach one node to every node
// ** MINIMUM SPANNING TREE(MST)(means wait of spanning tree is minimum)
